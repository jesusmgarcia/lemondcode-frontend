import * as React from 'react';
import { AppLayout } from 'layouts';
import { HomeComponent } from 'pods/home';

export const HomeScene = () => (
  <AppLayout>
    <HomeComponent />
  </AppLayout>
);
